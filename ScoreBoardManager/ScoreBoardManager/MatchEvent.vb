Public Class MatchEvent

    'If is starting the match
    Public Property StartMatch As Boolean

    'If match has finished
    Public Property EndMatch As Boolean

    'If is goal 
    Public Property IsGoal As Boolean

    'Minute when goal is scored
    Public Property Minute As Integer

    'Player name
    Public Property Player As String

    'Team name
    Public Property Team As String

End Class
